# Additional 50 tutoring pages — 2026-09-01

- Existing baseline: 101 Production tutoring URLs, 8,832 sitemap URLs.
- New batch: exactly 50 URLs in `additional-50-20260901.json`.
- Existing article data, original test-30 manifest, MASTER, image APIs and shared components are not changed.
- Four-segment manifest routing and daily ISR are retained. Only the new batch uses `AdditionalTutoringPage`.
- Local final check: 50 page responses and 50 PNG responses HTTP 200; 101 existing pages passed content/SEO/markup regression; sitemap 8,882 / tutoring 151.

## Content and evidence

The hand-authored learning profiles and worked examples are in `scripts/new50-content.mjs` and `scripts/new50-examples.mjs`. `build-new50-manifest.mjs` joins them with the existing region/school records. School facts use the stored February 2026 school basic-information snapshot. Exam advice and exercises are general teaching guidance, not assertions about a school's actual exam patterns or customer testimonials. Administrative town names may differ from legal town names in postal addresses.

Similarity is character 5-gram Jaccard on rendered prose paragraphs, with region/school names normalized. Repeated paragraphs within a page are deduplicated. Menu links, buttons and section headings shared by the required design are excluded; school facts and disclaimers remain. Each new page is compared against all 101 existing pages and the other new pages. Final local maximum: 0.19329608938547485 (19.33%). This is a reproducible lexical metric, not a claim of semantic uniqueness or guaranteed search ranking.

## Images

`render-new50-images.py` reads the MASTER without modifying it. Images are lossless 1254×1254 PNGs in `public/seo-images/20260901`. The original project-history Noto Sans KR font is used for replacement text; a raster reference cannot establish the original editable typeface. No AI generation is used.

Only declared region/subject glyph rectangles are edited, including the corresponding subject words in the subtitle and icon caption. Pixels outside these rectangles must be identical. The 50-image check reported zero changed pixels outside them. MASTER SHA-256: `fbd599fe2121cf5eef8138316b5bf1e44718cfe72a5f83366986c4adf639f4c6`.

## Verification

Run `npm run lint`, `npm run build`, then start a local production server. `node scripts/check-new50.mjs baseline` captures the pre-deployment live baseline (only when Production still has 101 tutoring URLs). `node scripts/check-new50.mjs local http://localhost:3100` verifies local rendering against it. After deployment run `node scripts/check-new50.mjs production` to verify the live URLs, images and pre-existing image hashes. Reports are saved under the OS temporary directory `studyhigh-new50-20260901`, not served by the website.

Do not append Google Sheets rows until the Production check succeeds. Do not regenerate or overwrite the pre-deployment baseline after publishing the batch.
